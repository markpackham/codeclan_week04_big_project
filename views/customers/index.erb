<h1>Customers</h1>
<div class="flex-container">
<div class="container">
<h2>Top 3 High Spenders</h2>
  <% for spender in @high_spenders %>
      <a href="/lessons/<%= spender.id %>"><%= spender.pretty_name() %></a> Funds: £<%= spender.funds %>
    <br>
  <% end %>
  <br>
  <h2>Top 3 Low Spenders</h2>
  <% for spender in @low_spenders %>
      <a href="/lessons/<%= spender.id %>"><%= spender.pretty_name() %></a> Funds: £<%= spender.funds %>
    <br>
  <% end %>
  <br>
  Average customer's funds are: £<%= @average_funds %>
  <br>
  All the customers' funds combined: £<%= @sum_funds %>
</div>
<div class="content">
<h2>All Customers</h2>
  <p>You currently have <%= @customers.length%> customers.</p>
  <% @customers.each do |customer| %>
  <a href="/customers/<%= customer.id %>"> <%= customer.pretty_name() %></a> - Unique ID: <%= customer.id %>
  <br>
  <% end %>
  </div>
</div>